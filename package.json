{
  "name": "cover-css",
  "version": "1.0.0",
  "description": "prueba",
  "main": "index.js",
  "scripts": {
    "deploy": "gh-pages -d dist",
    "start": "live-server src",
    "build": "npx parcel src/index.html --open",
    "dev": "npx parcel src/index.html --open",
    "correct": "npx stylelint src/css/*.css --fix",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./"
  },
  "keywords": [],
  "author": "Marlen",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "postcss-cli": "^7.1.0",
    "stylelint": "^13.1.0",
    "stylelint-config-standard": "^20.0.0"
  },
  "dependencies": {
    "gh-pages": "^2.2.0",
    "lodash-es": "^4.17.21"
  }
}
